version: '3.8'

services:
  petition-service:
    build: .
    container_name: petition-service
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - DB_DIR=/app/data
    restart: unless-stopped
